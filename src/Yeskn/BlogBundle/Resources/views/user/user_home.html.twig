{% extends '@YesknBlog/base.html.twig' %}

{% block main %}
    <div class="row">
        <div class="col-lg-12">
            <div class="panel-body" style="background: #fff;margin: 10px 0;">
                <ul class="chat">
                    <li class="left clearfix user-meta">
                        <span class="chat-img pull-left">
                              <img src="{{ user.avatar }}" alt="User Avatar" class="img-circle" width="50" height="50">
                        </span>
                        <div class="chat-body clearfix">
                            <div class="header">
                                <strong class="primary-font"><a href="{{ path('user_home', {username: user.username}) }}" data-pjax>{{ user.nickname }}</a></strong>
                                <small class="pull-right text-muted">
                                    <i class="fa fa-clock-o fa-fw"></i> {{ user.registerAt|date('Y-m-d H:i:s') }}
                                </small>
                            </div>
                            <p>{{ user.remark|raw }}</p>
                            <br/>
                            {% if app.user != user %}
                                <button type="button" data-toggle="modal" data-target="#exampleModal" class="btn btn-warning btn-sm"><i class="fa fa-envelope"></i> 发送私信</button>
                                <button type="button" id="follow-user" class="btn btn-outline btn-danger btn-sm"><i class="fa fa-heart"></i> {% if app.user in user.followers %}已经关注{% else %}特别关注{% endif %}</button>
                            {% endif %}
                          </div>
                    </li>
                </ul>
            </div>

            <div class="panel panel-default">
                <div class="panel-heading">
                    {{ user.nickname }}发布的文章
                </div>
                <div class="panel-body">
                    最近没有发布任何文章。
                </div>
            </div>
            <div class="panel panel-default">
                <div class="panel-heading">
                    {{ user.nickname }}的回复
                </div>
                <div class="panel-body">
                    最近没有发布任何回复。
                </div>
            </div>
        </div>
    </div>

    <script>
        $(function () {
            $(document).off('click', '#follow-user');
            $(document).on('click', '#follow-user', function () {
                $.ajax({
                    method: 'POST',
                    url: '{{ path("follow_user") }}',
                    data: {username: '{{ user.username }}'},
                    success: function () {
                        $.pjax.reload('.content-body', {
                            fragment: '.content-body',                          //来源也的ID
                            timeout: 200000000,
                            show: 'fade',
                            cache: true,  //是否使用缓存
                            push: true,
                            replace: true
                            //scrollTo: 250,
                        })
                    }
                })
            });

            $(document).off('click', '#send-message');
            $(document).on('click', '#send-message', function() {
                var $this = $(this);
                $this.button('loading');

                $.ajax({
                    method: 'POST',
                    url: '{{ path("send_message") }}',
                    data: {content: $('#message-text').val(), to: '{{ user.username }}'},
                    success: function () {
                        alert('发送成功');
                        $this.button('reset');
                        $('#message-text').val('');
                        $('#exampleModal').modal('hide');
                    }
                });
            });
        })
    </script>


{% endblock %}